# Desafio do Cap. 9
# A lista ja esta pronta. Sua missao: FILTRAR de outro jeito.
# 1) Complete a funcao comeca_com() usando .lower() e o metodo .startswith().
# 2) Use-a dentro do for para imprimir so as palavras que comecam com a letra "a".

palavras = ["gato", "abelha", "arara", "ceu", "abacaxi", "rio"]


def comeca_com(palavra, letra):
    # TODO: devolva True se 'palavra' (em minusculas) comecar por 'letra'.
    #       Dica: use palavra.lower().startswith(letra)
    return False


print("Palavras que comecam com 'a':")
for palavra in palavras:
    # TODO: chame comeca_com(palavra, "a") dentro de um if e imprima a palavra.
    pass
