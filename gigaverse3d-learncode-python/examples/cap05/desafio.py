# Desafio do Cap. 5
# Escreva uma função RECURSIVA chamada soma_ate(n) que devolve
# a soma de todos os inteiros de 1 até n (ex.: soma_ate(3) = 1 + 2 + 3 = 6).
#
# Lembre-se das duas partes de toda recursão:
#   - CASO-BASE: o n mais simples, em que a resposta é imediata (n == 0 -> 0).
#   - CASO RECURSIVO: n + a soma de tudo que vem antes (n + soma_ate(n - 1)).


def soma_ate(n):
    # TODO: escreva o caso-base (quando n == 0, devolva 0)

    # TODO: escreva o caso recursivo (devolva n + soma_ate(n - 1))
    pass


# Quando terminar, descomente as linhas abaixo e rode:
# print("soma_ate(3) =", soma_ate(3))   # esperado: 6
# print("soma_ate(5) =", soma_ate(5))   # esperado: 15
