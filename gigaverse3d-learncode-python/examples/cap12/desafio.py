# Cap. 12 - Desafio: soma e média numa tupla só
# 1) Complete a média (total dividido pela quantidade de itens).
# 2) Faça a função RETORNAR a tupla (total, media).
# 3) Desempacote o retorno em duas variáveis e rode para conferir.

def soma_e_media(numeros):
    total = sum(numeros)
    # TODO: calcule a média usando total e len(numeros).
    media = 0  # troque esta linha pelo cálculo correto
    # TODO: retorne a tupla (total, media) no lugar de None.
    return None


valores = [10, 20, 30, 40]
# TODO: desempacote o retorno em duas variáveis: soma, media
resultado = soma_e_media(valores)
print("Resultado:", resultado)
